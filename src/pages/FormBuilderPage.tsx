// src/pages/FormBuilderPage.tsx
/*
import React from 'react';
import { FormBuilder } from '../components/form-builder/FormBuilder';
import { FHIRConverter } from '../core/FHIRConverter';

export const FormBuilderPage: React.FC = () => {
  const handleExport = async (formData: FormItem[]) => {
    const converter = new FHIRConverter();
    const fhirQuestionnaire = converter.convertToFHIR(formData);
    // Handle FHIR output
  };

  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold mb-4">FHIR Questionnaire Builder</h1>
      <FormBuilder onExport={handleExport} />
    </div>
  );
};*/